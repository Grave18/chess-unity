<UserControl x:Class="Ui.Menu.Pages.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="640" d:DesignHeight="1080">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Main Menu" Style="{StaticResource Header}"/>
        <Button Grid.Row="1" Content="Play" Click="Play_Click" />
        <Button Grid.Row="2" Content="Settings" Click="Settings_Click" />
        <Button Grid.Row="3" Content="Exit" Click="Exit_Click" />
    </Grid>

</UserControl>