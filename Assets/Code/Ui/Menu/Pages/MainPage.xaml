<UserControl x:Class="Ui.Menu.Pages.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="640" d:DesignHeight="1080"
             Focusable="True"
             IsTabStop="False">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="*" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Main Menu"
                   Style="{StaticResource HeaderTextBlockStyle}" />
        <StackPanel Grid.Row="1" Margin="0 50 0 0">

            <!-- Special style for Main page Buttons screen -->
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource GeneralButtonStyle}">
                    <Setter Property="Margin" Value="0 20 0 20" />
                </Style>
            </StackPanel.Resources>

            <Button Content="Play" Click="Play_Click" />
            <Button Content="Settings" Click="Settings_Click" />
            <Button Content="Board" Click="Board_Click" />
        </StackPanel>

        <!-- Exit Button -->
        <Button Grid.Row="2"
                Name="ExitButton"
                Content="Exit"
                KeyDown="ExitButton_OnKeyDown"
                Click="Exit_Click" />


        <!-- Popup -->
        <Popup Grid.Row="0"
               x:Name="Popup"
               Focusable="True"
               Style="{StaticResource PopupStyle}">
            <Viewbox Stretch="Uniform">
                <Grid Style="{StaticResource UniformGridStyle}">
                    <Border Style="{StaticResource PopupBorderStyle}">
                        <DockPanel Name="PopupDockPanel"
                                   Margin="30"
                                   KeyboardNavigation.TabNavigation="Contained"
                                   KeyboardNavigation.DirectionalNavigation="Contained">
                            <TextBlock DockPanel.Dock="Top"
                                       Name="PopupText"
                                       Style="{StaticResource Header2TextBlockStyle}" />
                            <Grid DockPanel.Dock="Bottom"
                                  VerticalAlignment="Bottom">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <!-- Cancel Button -->
                                <Button Grid.Column="0"
                                        Name="PopupCancelButton"
                                        Content="Cancel"
                                        Click="PopupClose_Click"
                                        Style="{StaticResource PopupButtonStyle}"/>
                                <!-- Yes Button -->
                                <Button Grid.Column="1"
                                        Name="PopupYesButton"
                                        Click="PopupYes_Click"
                                        Style="{StaticResource PopupButtonStyle}"/>
                            </Grid>
                        </DockPanel>
                    </Border>
                </Grid>
            </Viewbox>
        </Popup>

    </Grid>

</UserControl>